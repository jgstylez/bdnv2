/**
 * useFeatureFlags Hook
 * 
 * Convenience hook for accessing feature flags throughout the app.
 * Re-exports the context hook for easier imports.
 */

export { useFeatureFlags } from '@/contexts/FeatureFlagsContext';

